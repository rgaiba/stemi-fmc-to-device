Drop here: `acs5_2023_b01001_bg.csv` (~5.3 MB, ~238k rows; columns `bg_id`, `total_pop_acs`, `adult_pop_20plus`).

Generated by: `python national/src/01b_prepare_acs_age.py` (no arguments). Pulls ACS 2019-2023 5-year table B01001 at block-group resolution from the Census Data API for 49 CONUS entities and sums the 36 male+female age bands aged 20+ to produce the adult-population-by-BG denominator used in the STEMI rate multiplication.

Sandbox-blocked: the cowork sandbox proxy blocks Census endpoints, so this script is run on the user's local machine; the resulting CSV lives in this directory and is read from here by sandbox-side downstream steps.

SHA256 of the canonical CSV (run of 2026-05-08): `c89eec1df13a54781e3a9a838dae464e06b65fb93e4bc7d1716032ddeaaae2cd`. The script also writes this checksum to `SHA256.txt` (gitignored alongside the CSV).

See `national/data/MANIFEST.md` §5 for full provenance, validation table, and citation.
